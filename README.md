# A Discord Bot for use in D&D sessions

## Installation

1. clone with git
2. navigate to root of git repo
3. pip install discord.py\[voice\] -t ./site-packages
4. add a .env file with your Discord bot's token
5. run bot.py with python
